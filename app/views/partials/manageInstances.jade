div(ng-controller='manageInstCtrl')

  h3.section-title.text-center.wow.fadeInDown Instances existantes
  
  div(style='margin-bottom: 80px;')
    .span.btn.btn-primary.pull-left(role='button', ng-click="backToPoll()") Retour au sondage
    .h4.wow.fadeInDown.text-right(ng-if="instances.length < 1") Aucune instance n'existe pour ce sondage !

    
  table.table.table-hover(ng-if="instances.length > 0")
    thead
      tr
        th Instances existantes
        th Résultats
        th Supprimer
      tbody(ng-repeat='instance in instances')
        tr.clicker
          th {{ instance.name }}
          td
            span.btn.btn-xs.btn-info.instance(role='button', ng-click="showResults(instance.id)") Voir les résultats
          td
            span.btn.btn-xs.btn-danger.instance(role="button", ng-click="deleteInstance(instance.id)") Supprimer l'instance
  
  form-horizontal
        label.control-label
        col-xs-10(style='padding-right: 0px;')
          input.form-control(type='text', placeholder='Nom de la nouvelle instance', ng-model='instanceName')
          span.btn.btn-primary.pull-right.instance(style='margin-top: 20px;')(role='button', ng-click="addInstance()") Ajouter une instance



